{
  "name": "fleet-fuel-tracker",
  "version": "1.0.0",
  "description": "Sistema de control de kilometraje y horómetro para flota vehicular - Demo con datos mockeados",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "build": "echo 'Static demo - no build required'",
    "vercel-build": "echo 'Static deployment - skipping build step'",
    "serve": "node server.js"
  },
  "private": true,
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "@vercel/analytics": "^2.0.1"
  }
}
